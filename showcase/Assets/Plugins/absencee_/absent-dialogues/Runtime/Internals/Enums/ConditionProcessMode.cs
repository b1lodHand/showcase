namespace com.absence.dialoguesystem.internals
{
    /// <summary>
    /// An enum used to define the way to handle multiple variable manipulators at once.
    /// </summary>
    public enum ConditionProcessMode
    {
        All = 0,
        Any = 1,
    }
}